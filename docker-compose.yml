services:
  bot:
    build: .
    container_name: instasavebot
    restart: unless-stopped
    env_file:
      - .env
    stop_grace_period: 30s
    volumes:
      - ./data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
